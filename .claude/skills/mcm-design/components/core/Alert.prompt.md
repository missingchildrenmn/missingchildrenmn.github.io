Use Alert for destructive confirmations and blocking messages in KidsIdKit flows.

```jsx
<Alert header="Remove Ava?" message="This deletes Ava's record from this device." confirmPrompt="Yes" cancelPrompt="No" />
```

It positions absolutely inside the nearest positioned ancestor — put it inside the phone frame, not the page body.
