/** Translucent blocking spinner, matching BusyIndicator (ion-loading). */
export interface BusyOverlayProps {
  show?: boolean;
  message?: string;
}
export function BusyOverlay(props: BusyOverlayProps): JSX.Element | null;
