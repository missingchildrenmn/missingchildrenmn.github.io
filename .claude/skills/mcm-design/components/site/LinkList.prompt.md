Use LinkList for the Resources page and the app's Information screens — phone numbers go in `note`.

```jsx
<LinkList spacing="touch" items={[{label:'National Center for Missing and Exploited Children', note:'1-800-THE-LOST'}]} />
```
