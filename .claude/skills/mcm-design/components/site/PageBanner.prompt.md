Use PageBanner directly under the page `h1`, matching the live site.

```jsx
<PageBanner src="assets/imagery/support-banner.jpg" height={200} />
```

Banners are cool teal-toned, grainy, tightly cropped photographs. Never overlay text on them — MCM does not.
