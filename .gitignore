_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
Gemfile.lock

# Per-machine Claude Code state. The rest of .claude/ (the mcm-design skill) is
# committed on purpose so it travels with the repo.
.claude/settings.local.json
